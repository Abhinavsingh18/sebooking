[build]
  publish = "build/web"
  command = "flutter build web --release"

# Proxy API requests to avoid Mixed Content errors
[[redirects]]
  from = "/api/*"
  to = "https://api.sebooking.in/:splat"
  status = 200
  force = true

# SPA Fallback (Must be last)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
